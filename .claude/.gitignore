# Ignore all files by default in .claude directory
*

# Only allow specific safe files
!.gitignore
!settings.json
!keybindings.json
!install.sh
!README.md
!scripts/

# Never commit these (extra safety)
settings.local.json
history.jsonl
stats-cache.json
session-env/
debug/
file-history/
todos/
projects/
plugins/
downloads/
plans/
ide/
